# case

C implementation area for case boundary, posture and evidence refs.

`case_world.c` is the first verticalized case import: it materializes
case-domain, case-attachment, and case-binding residue without importing the
broader registry/runtime stack yet.
