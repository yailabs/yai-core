# ctl

`yai` is the canonical technical control command for `yai-core`.

Transitional role: this root is a bootstrap pointer only. The current Rust
implementation now lives in:

```text
cmd/yai
```

Retired role: this root is no longer an implementation root. It remains only as
a short historical pointer until compatibility drains.

```text
cmd/yai
```

Local install layout is delayed to NEW.20.

Console UX belongs outside this repository.
