# projection

C bootstrap implementation for controlled projections.

No UI state belongs here.

Target role:

```text
lib/projection -> system/engine_bridge + engine/yai-engine/src/projection
```

Owning wave: NEW.18 moves projection materialization toward Rust and leaves
only bridge behavior in C.
