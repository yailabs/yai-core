# Engine

Status: NEW.14 Rust operational data engine location.

This root owns the Rust data spine workspace after the NEW.14 move:

```text
engine/yai-engine
engine/yai-engine-ffi
```

`crates/` remains transitional only for `yai-ctl` until NEW.15 moves the
command to `cmd/yai`.
