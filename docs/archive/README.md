Historical/superseded engineering record. Not an active source of truth.

# Archive

This directory contains historical records only.

Active doctrine, roadmap, command surface, source surface and validation rules
live outside `docs/archive/`.

Archived documents may preserve old repository names, old wave names, old path
plans and superseded terminology because they record what was true at the time
of the archived wave. They must not be used as active authority for future
implementation.

Current active repository identity:

```text
YAI/yai     = canonical local AI operational control system
YAI/yai-dev = development lab, concept mine, harness and scenario workspace
```

If an archived file is useful for a future wave, extract the concept into the
active docs or implementation and update `docs/internal/extraction-inventory.tsv`.
