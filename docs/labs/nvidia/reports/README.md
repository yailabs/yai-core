# NVIDIA Reports

Curated NVIDIA LAN benchmark reports belong here. Each report directory should
include an `index.md`, normalized data and assets generated from selected raw
runs.

Raw benchmark folders stay under ignored `benchmarks/nvidia/`.
