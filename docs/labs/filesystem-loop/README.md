# Filesystem Loop Lab

Status: canonical operator lab for filesystem-loop validation.

Purpose: validate the command surface, daemon loop, case residue,
policy-pack materialization posture and optional LAN provider attachment.

This lab replaces `docs/manuals/manual-filesystem-loop-validation.md` as the
source of truth. The files under `docs/manuals/` are compatibility redirects.

## Standard Files

| Concern | Path |
|---|---|
| Purpose and map | `docs/labs/filesystem-loop/README.md` |
| Runbook | `docs/labs/filesystem-loop/runbook.md` |
| Tests | `docs/labs/filesystem-loop/tests.md` |
| Output contract | `docs/labs/filesystem-loop/outputs.md` |
| Lightweight outputs placeholder | `docs/labs/filesystem-loop/outputs/` |
| Curated reports | `docs/labs/filesystem-loop/reports/` |
| Policy-pack fixtures | `docs/labs/filesystem-loop/policy-packs/` |

## Boundary

This lab is not a benchmark and is not provider support. It validates that YAI
keeps provider output as claim/proposal material inside a case boundary.
