# Normalized

Normalized artifacts derived for this historical baseline run live here.

Current contents:

- `normalized-summary.json`

These files are derived summaries only. They do not contain new model output.
