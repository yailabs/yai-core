# Model Behavior Lab Artifacts

This directory contains curated lab artifacts for model behavior inside YAI
cases.

These files are:

- representative local runs
- useful for architecture/debug review
- not benchmarks
- not security guarantees
- not conformance fixtures

Raw local logs should not be committed. Public artifacts must sanitize:

- absolute paths
- local ports where needed
- secrets or env values
- broken encoding
