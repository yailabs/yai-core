# case

C ABI declarations for case identity, boundary, posture and evidence refs.

`case_ref.h` is the small case identity handle.

`case_world.h` is the first imported case-world slice from the broader YAI case
model: case domain, case attachment, and case operational binding. It keeps the
case as an operational world, not just a label on journal records.
