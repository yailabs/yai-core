# Case Runtime Discovery

SPINE.51A maps current case runtime semantics before implementing
CaseHandle, CapabilityLease, ResourceScope, VisibilityScope or AuthorityScope.

Files:

```text
case-runtime-discovery.md
ref-binding-authority-map.tsv
yai-dev-extraction-candidates.tsv
implementation-gap-list.md
next-wave-recommendation.md
```

This is discovery-only material. It changes no `yai` runtime behavior and does
not mutate `yai-dev`.
