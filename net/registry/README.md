# registry

Owns future node, endpoint and capability registry shapes.

Must not resolve YAI policy, discover networks or persist authoritative YAI
state.

Current status: scaffold.

Next expected wave: NET.SPINE.4 capability registry seed and NET.SPINE.5 local
endpoint registry skeleton.
