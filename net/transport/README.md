# transport

Owns future transport adapter boundaries for local, localhost, LAN and remote
paths.

Must not implement sockets, HTTP servers, discovery, connect/listen/bind
behavior or CLI control in this scaffold.

Current status: scaffold.

Next expected wave: NET.SPINE.8 localhost adapter skeleton and later transport
adapter interface work.
